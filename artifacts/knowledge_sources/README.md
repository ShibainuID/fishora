# Fishora Knowledge Sources

Controlled offline corpus for the Fishora RAG pipeline. This directory is the
handoff boundary between externally run research agents and the operator CLI;
**the main API never runs any corpus command**.

## Directory layout

| Path | Status | Meaning |
|---|---|---|
| `offline/research.json` | offline input | Source identification (research agent group 1) |
| `offline/fact_extraction.json` | offline input | Claim formalization (research agent group 2) |
| `offline/verification.json` | offline input | Source revalidation (research agent group 3) |
| `offline/knowledge_editor.json` | offline input | Editor handoff; its records become candidate chunks |
| `candidates/*.json` | `candidate` | One record per chunk: `claim_id` + `chunk` + `source` (title, publisher, URL, support quote) |
| `approved/*.json` | `verified` | Created **only** by the approval action; never auto-approved |
| `approval-manifest.json` | — | Signed by the approval action (HMAC-SHA256 over the canonical manifest plus approved-file hashes); consumed by `require_approved_manifest` |
| `review/*.json` | operator input | Human review file (see below) |

The four offline stage files are static handoff artifacts. They were written
once by the research agent groups and are validated by the CLI, not
regenerated by any script in this repository.

## Mandatory human approval

Agents (research, fact-extraction, verification, knowledge editor) can only
produce `candidate` records, and candidate sources carry **no** `reviewed_at`
or reviewer metadata. Only the CLI approval action may create `verified`
copies, and it requires **all** of:

1. a non-empty `--reviewer`,
2. the exact confirmation token `APPROVE` (never any other string),
3. the `FISHORA_CORPUS_APPROVAL_KEY` signing key (missing/blank rejected),
4. explicit `approved_chunk_ids` **and** `approved_source_ids` in the review file,
5. a per-source human attestation (`source_reviews` entry with reviewer name
   and `reviewed_at` for every approved source).

```text
python3 -m scripts.corpus_pipeline collect \
  --stage-dir artifacts/knowledge_sources/offline \
  --candidate-dir artifacts/knowledge_sources/candidates

FISHORA_CORPUS_APPROVAL_KEY=... python3 -m scripts.corpus_pipeline approve \
  --candidate-dir artifacts/knowledge_sources/candidates \
  --review-file artifacts/knowledge_sources/review/approval.json \
  --approved-dir artifacts/knowledge_sources/approved \
  --approval-manifest artifacts/knowledge_sources/approval-manifest.json \
  --reviewer operator \
  --confirmation APPROVE
```

`approve` requires the `FISHORA_CORPUS_APPROVAL_KEY` environment variable: the
HMAC-SHA256 key that signs the approval manifest. The signed manifest wraps the
canonical manifest body together with SHA-256 hashes of the approved record
files; `require_approved_manifest` recomputes both and compares with
`hmac.compare_digest`, rejecting unsigned manifests, altered files, hand-written
verified records and modified IDs. The key is never stored or logged.

Review file shape (attestations are written into the approved source copies
and cannot be forged afterwards):

```json
{
  "approved_chunk_ids": ["chunk_bandeng_identity_001"],
  "approved_source_ids": ["fishbase_chanos_chanos"],
  "source_reviews": {
    "fishbase_chanos_chanos": {"reviewer": "operator", "reviewed_at": "2026-08-24T08:00:00Z"}
  }
}
```

`collect` requires all four stage files and validates **every** record in
every stage file (stage matches the filename, non-empty content and source
quote, safe IDs, declared sources, `reviewed_at` exactly `None` on candidate
sources) plus full cross-stage lineage: each
`knowledge_editor` record must have matching research, fact-extraction and
verification records with the same (`claim_id`, `source_id`), and vice versa.
Unsupported labels/categories and non-candidate statuses are rejected.
`collect` never writes to an approved directory or database.

Security: claim/chunk/source IDs must match `^[a-z0-9_]+$`; every candidate
and approved path is resolved under its intended root; symlinks and path
escapes are rejected before any read or write; `candidate_dir` and
`approved_dir` must be distinct non-overlapping roots.

## Corpus

- 11 labels, **33 candidate chunks**, **20 sources** (FishBase, FAO, and
  peer-reviewed journal articles; all URLs revalidated 2026-08-23).
- Categories: `identity`, `physical_characteristics`, `taste_texture`,
  `processing_methods`, `commercial_uses`, `substitutes` (exactly these six).
- Every claim's `source_quote` is an exact supporting excerpt from the cited
  URL (page text or article abstract). No bibliographic-only strings are used.
- **tenggiri**: the vernacular name is shared by five species (FishBase common
  names); candidate chunks document the ambiguity and the two main candidate
  species, `Scomberomorus commerson` and `S. guttatus`.
- **tuna**: genus-level only (`Thunnus`, seven species per FAO Scombrids
  catalogue); no single species is asserted.
- **gembolo**: unresolved; the only record is the limitation that the FishBase
  common-name search for 'Gembolo' returns no entry, so no scientific identity
  can be assigned without expert confirmation.
- Where evidence was unavailable (e.g. the gulamah surimi article is
  login-gated and could not be verified), the factual claim was removed rather
  than kept with an unsupported quote.

## Approval-boundary tests

`apps/main_api/services/corpus.py` enforces the approval boundary and is
exercised by `tests/main_api/test_corpus.py` / `test_corpus_cli.py`,
including path-traversal/symlink attacks, forged-manifest rejection, and the
main-API isolation scan.